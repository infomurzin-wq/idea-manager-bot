# UFC Weekly Monitoring Report

## Турнир

- Турнир: `<event name>`
- Дата: `<YYYY-MM-DD>`
- Основной источник: `<source>`
- Сформировано: `<timestamp>`

## Сводка по карте

- Всего боёв: `<count>`
- Боёв основного карда: `<count or n/a>`
- Боёв прелимов: `<count or n/a>`
- Качество данных: `<good / partial / weak>`

## Легенда по форме

- `🟩 W` — победа
- `🟥 L` — поражение
- `🟨 D` — ничья
- `⬜ NC` — no contest

## Бои

### `<fighter A>` vs `<fighter B>`

`<weight class>` | `<status>` | `<card segment>`

#### Линии

- `<fighter A>`: `<decimal moneyline or n/a>`
- `<fighter B>`: `<decimal moneyline or n/a>`
- `ТБ 1.5`: `<decimal or n/a>`
- `ТБ 2.5`: `<decimal or n/a>`

#### `<fighter A>`

- Рекорд: `<wins-losses-draws>`
- Победы по методам: `<summary>`
- Поражения по методам: `<summary>`
- Источники: `<sources>`

Последние 5:

| Дата | Соперник | Результат | Метод | Раунд | Промоушен | Ивент |
| --- | --- | --- | --- | --- | --- | --- |
| `<YYYY-MM-DD>` | `<name>` | `<🟩 W/🟥 L/🟨 D/⬜ NC>` | `<method>` | `<round>` | `<promotion>` | `<event>` |

Короткий комментарий:

- `<русский комментарий по бойцу>`

Предбоевые сигналы:

- `<существенный сигнал 1 или "Существенных предбоевых сигналов не найдено.">`
- `<источник/ссылка или n/a>`

#### `<fighter B>`

- Рекорд: `<wins-losses-draws>`
- Победы по методам: `<summary>`
- Поражения по методам: `<summary>`
- Источники: `<sources>`

Последние 5:

| Дата | Соперник | Результат | Метод | Раунд | Промоушен | Ивент |
| --- | --- | --- | --- | --- | --- | --- |
| `<YYYY-MM-DD>` | `<name>` | `<🟩 W/🟥 L/🟨 D/⬜ NC>` | `<method>` | `<round>` | `<promotion>` | `<event>` |

Короткий комментарий:

- `<русский комментарий по бойцу>`

Предбоевые сигналы:

- `<существенный сигнал 1 или "Существенных предбоевых сигналов не найдено.">`
- `<источник/ссылка или n/a>`

Комментарий по бою:

- `<русский комментарий по стилям, форме, темпу, betting-context и тому, как предбоевые сигналы могут влиять на бой>`

## Что изменилось

Используется только для пятницы и субботы.

- `<change 1>`
- `<change 2>`
- `<change 3>`

## Примечания

- Отсутствующие поля: `<if any>`
- Fallback-источники: `<if any>`
- Блок предбоевых сигналов основан только на существенных и проверяемых источниках.
